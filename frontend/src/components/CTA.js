import '../styles/sections.css'

export default function CTA() {
    return (
        <section className="cta">
            <h2>Ready to get things done?</h2>
            <p>Stop switching between tools. One agent handles everything.</p>
            <button className="cta-btn magnetic">Start for free →</button>
        </section>
    )
}
